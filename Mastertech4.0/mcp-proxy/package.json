{
  "name": "mastertech-mcp-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Persistent stdio MCP proxy that forwards to Mastertech's :9004 MCP and auto-reconnects across recompiles.",
  "bin": {
    "mastertech-mcp-proxy": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^7.0.2"
  }
}
