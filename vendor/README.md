# Vendored crates

| crate | source | local changes |
|---|---|---|
| `zc-codex-client` | `WorkProjects/zc-codex` `src/lib.rs` | daemon module left out; keep changes mirrored in that repo |

`egui-snarl` and `egui-scale` were vendored here until crates.io published egui 0.36 releases (0.12.0 and 0.6.1); they now come from crates.io.
