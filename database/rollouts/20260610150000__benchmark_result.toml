id = "20260610150000__benchmark_result"
name = "benchmark_result"
state = "completed"
# Source: target asserted by the last completed rollout (ai_chat).
source_schema_hash = "47492ad4fc251319fecf6c6c7d1491461567648e77a2bf6c200a974db7e39bcd"
# Target: on-disk hash after adding benchmark_result.surql. Run
# `surrealkit rollout lint database/rollouts/20260610150000__benchmark_result.toml`
# and paste the reported hash here if lint disagrees.
target_schema_hash = "2202dc2359631a22ada0373dac28448b89ca108cad1b60ff773993a5660089c1"
compatibility = "phased"
renames = []

# Pure-additive: one new SCHEMALESS table for scored benchmark results
# (stress-runner benchmark module + Benchmark:* remote scripts). Mirrors the
# ai_chat precedent: apply the schema file verbatim, no OVERWRITE.
[[steps]]
id = "apply_benchmark_result"
phase = "start"
kind = "apply_schema"
files = [
    "database/schema/benchmark_result.surql",
]
entities = []

[[steps]]
id = "rollback_benchmark_result"
phase = "rollback"
kind = "remove_entities"
files = []

[[steps.entities]]
kind = "table"
name = "benchmark_result"
